# Allow netd to configure network interfaces in sysfs and procfs
allow netd sysfs_net:dir { add_name write };
allow netd sysfs_net:file create;
allow netd proc_net:dir { add_name write };
allow netd proc_net:file create;
